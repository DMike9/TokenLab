# Used ONLY by the optional local Node gateway, never by browser code.
# Copy to .env.local. Never use a VITE_ prefix for a secret.
GEMINI_API_KEY=
# Set a text model available to your Gemini project. See Google's models page.
GEMINI_MODEL=gemini-3.8-flash
